/** @type {import('next').NextConfig} */
const nextConfig = {
  // скрываем dev-индикатор Next.js, чтобы он не перекрывал блок пользователя в сайдбаре
  devIndicators: false,
};

export default nextConfig;
